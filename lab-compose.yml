name: home-lab

networks:
  home-lab-net:
    name: home-lab-net

services:
  home-lab-adguard:
    image: adguard/adguardhome:latest
    container_name: home-lab-adguard
    restart: unless-stopped
    ports:
      - 53:53/tcp
      - 53:53/udp
      - 23001:3000/tcp
      - 23002:80/tcp
    volumes:
      - ${ADGUARD_CONFIG}/work:/opt/adguardhome/work
      - ${ADGUARD_CONFIG}/conf:/opt/adguardhome/conf
    networks:
      - home-lab-net
